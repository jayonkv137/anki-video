# Project Status

> Where we are, what's done, where we left off. Update at the end of every working session.

## Milestone: MVP (see planning/PROJECT_GOAL_AND_MILESTONES.md §3)

## Phase checklist (V2)

- [x] V1: Plan + Setup + B0 (n8n) + B1 (word source) + B2 (story stage)
- [x] 2026-07-14 PIVOT → V2 Instagram content pipeline (docs re-cut)
- [~] **C1 — Character & Art Bible**: character sheets ✓, behavior bible ✓, **visual identity LOCKED as photorealistic-CGI material laws (canon_blocks v1.0, 2026-07-21 pivot)**. Remaining: style reference image (style-lock), C1 win-condition test (two independent generations of the same character indistinguishable — first evidence comes from Jayon's manual Flashboard tests).
- [~] C2 — Screenplay chain: **E1–E6 DONE** (canon system, ledger, pipeline with Gate A, skills v2, dual-package prompts, multiple full live runs). E7 reframed: prove the full loop before perfecting story quality (idea #16 parked).
- [~] C3 — Video prototyping: models chosen (Seedance 2.5 + Omni). **Provider system BUILT** (`pipeline/providers/video.py`): `mock` (runs now, free) + `fal` real-Seedance adapter (key-gated, UNVERIFIED — never actually called). `pipeline generate/autopilot` proven end-to-end on mock. **Voice refs incorporated (Path A)** — sheet+portrait+voice per character, auto-attached. **Manual real-Seedance test IN PROGRESS**: fresh run `51cc85bb` → `output/episodes/ep_54-564/flashboard_voice.md` (10 cap-compliant prompts, no style ref, dual char refs + voice mp3). Remaining: style-lock image, verify the real `fal` call, Jayon's test results.
- [~] C5 — Assembly: **BUILT** — `pipeline assemble` (normalize → concat → burned DE/EN subtitles → final.mp4). Post-chain (upscale/LUT/grain) parked as idea #17.
- [~] C6 — Publishing: caption BUILT (skill-4). **Command Center (M7) BUILT** — full dashboard w/ Gate A + post studio + autopilot. Remaining: real IG posting adapter, Gate 2 in UI.
- [ ] C4 — n8n port / scheduler (M8, optional — Python pipeline already orchestrates) · C7 — Daily ops (MVP done)
- [~] **M9 productization research DONE** — `docs/planning/RESEARCH_market_and_prior_art.md` (prior art: ViMax/n8n templates/Wireflow; differentiator = governance layer; Duolingo anti-slop validation; target markets ranked).

## PARKING LOT → moved to docs/planning/IDEAS_PARKING_LOT.md (system doc)

## Where we left off

→ **LATEST HANDOFF: docs/handoffs/HANDOFF_2026-07-22_voice-refs-and-machine-half-complete.md — new sessions run `/pickup` and read this first.**
→ **FORWARD ROADMAP: docs/planning/MVP_ROADMAP_command_center.md** · **REAL-API connection: docs/planning/REAL_API_CONNECTION.md** · **market research: docs/planning/RESEARCH_market_and_prior_art.md** · **voice refs: docs/planning/VOICE_REFS_INCORPORATION_PLAN.md**.
→ **⚠ ANTHROPIC API CREDITS EXHAUSTED.** Every LLM pipeline stage (`run`, `choose`, prompt regen, `caption`) is blocked until Jayon tops up billing. Confirmed via a live 400 error this session. Check this FIRST before attempting any pipeline command.
→ **THE MACHINE HALF OF THE MVP IS BUILT (all on `main`, merged, NOT yet pushed to origin — 22 commits ahead).** Full pipeline: `run` → Gate A → `choose` → screenplay → QC → dual CGI prompts → `generate` (mock|fal) → `assemble` (subtitles) → `caption`; or `autopilot` for the last three in one command. Provider system (`pipeline/providers/`) makes real Seedance a one-key swap (`FAL_KEY`) — **but the real fal.ai call has never actually been executed; the adapter's model slug + arg names are unverified.** Command Center dashboard live at `localhost:8787` (`.venv/bin/python -m uvicorn dashboard.app:app --port 8787`). Mock full-loop proven end-to-end (subtitled 69s video, frame-verified, ep_22-499).
→ **Per-character VOICE references incorporated (Path A, 2026-07-22).** Each character now resolves to sheet+portrait+voice `.mp3`, auto-attached to every scene (verified via resolver test + synthetic manifest — NOT via a live LLM regen). skill-3 v3.1 / seedance canon v2.1 / omni canon v1.2 updated to emit `Use @AudioN as the voice of <Name>` bindings. **Path B (future: per-scene ElevenLabs dialogue) fully speced, not built** — see the plan doc.
→ **CRITICAL PATH — Jayon's manual Seedance visual test (M1), IN PROGRESS.** Fresh run `51cc85bb` "Der fünfzigste Spieltag" (Müller×Rolf, garden/matchday) → **`output/episodes/ep_54-564/flashboard_voice.md`** (SUPERSEDES the earlier `flashboard.md`): 10 prompts, all ≤3000 chars, **NO style ref**, each character bound to **main image + character sheet + voice mp3**. ⚠ These 10 prompts were hand-transformed from older generated text (regex), NOT natively produced by skill-3 v3.1 — re-verify shape once credits are restored. Outcome decides downstream: materials render photoreal → continue; AI-gloss/claymation → `/tune` canon_blocks v1.1.
→ **REMAINING to MVP = external-API keys + creative steps only:** verify real `fal` Seedance call, style-lock image, ElevenLabs voices (Path B), real IG posting adapter + Gate 2, optional n8n scheduler.
→ **Known watch-items:** skill-3 still needs manual trimming to hit the 3000-char cap on voice+dual-image 2-char scenes (a permanent /tune is owed); caption once emitted an off-brand `#germanpuppets` tag; QC (Haiku) keeps failing on dialogue-naturalness nits across MULTIPLE runs now and proceeding per design — accumulating signal that skill-2 itself needs a `/tune`, not just per-episode fixes; Bert's "felt hat" vs AVOID "felt" collision untested; an unexplained completed run `575d3158`/`ep_14-456` exists in the ledger with no session memory of creating it — investigate if it matters.

2026-07-21 (latest): **Machine half of MVP built + M9 research + fresh test run.** Built the full back half: video provider system (mock + real Seedance/fal adapter), `pipeline generate`, `assemble` (subtitles), `caption` (skill-4), `autopilot`, and the **Command Center dashboard** (FastAPI + browser-verified) — the loop is fully operable from one screen. Mock autopilot proven end-to-end (subtitled 69s video). Ran deep **M9 market/prior-art research** (differentiator = governance layer; Duolingo anti-slop validation). Then a **fresh proper run** for Jayon's manual Seedance test: run `51cc85bb` → chose option 3 → `ep_54-564/flashboard.md` with 10 cap-compliant prompts, no-style + dual-character-ref config. Awaiting his Flashboard render results (M1 critical path). All merged to `main`.

2026-07-21 (later): **THE VISUAL PIVOT** — puppet → photorealistic CGI live-action integration, from Jayon's three research docs (Prompting Consistency, AI Video Pipeline Design, CGI Integration Lookbook). canon_blocks v1.0 material laws (Bert IOR/caustics, Kati matte-albedo, Rolf subsurface-scattering, Müller displacement/AO); lighting+DoF moved from permanent canon to per-scene variables written by skill-3 v3.0 (virtual Director of Photography); Live-Action Integration Rule everywhere; identity refs now sheet-first dual (sheet=structural map, portrait=close-up anchor); regression run verified the pivot + caught the 3000-char cap overrun (budget rule + warning added). REGISTRY v1.2.

2026-07-21 (earlier): **E6 complete** — first full pipeline run end-to-end (Gate A → episode.md), 3 bugs fixed along the way: skill-3 `max_tokens` truncation (24k→64k for the dual-package output), `ledger.add_cost` 10x overcount + wrong-tier pricing (now per-model rate table), `choose`/`resume` reloading the wrong words for `--random` runs (`fetch_words_by_positions`). Also this session: tuned `Characters-Main-Sheet.md` v1.2 + skill-2 v1.1 to fix an observed "quiet characters became mute" pattern (Müller/Rolf) — verified in the live run: Müller spoke real dialogue in all 10 scenes.

2026-07-21: E5 skills v2 shipped + committed (`491fd61`, branch `feat/e5-skills-v2`) — skill-1 split into 1a (Gate A options) / 1b (expand); skill-2q quality-check on Haiku 4.5 (code validators + LLM checklist, verdict → ledger); skill-3 v2 dual Seedance/Omni packages; version headers on all skills; `_call` model/max_tokens parameterized.

2026-07-20: E1-E4 completed in Antigravity (Opus) session. E1: prompting canon distilled (Seedance + Omni guidelines, ≤100 lines each, Jayon-approved). E2: MISSION.md + REGISTRY.md (hash-verified canon). E3: Supabase ledger tables (runs, run_events, episodes) + migration of episode_log.json. E4: pipeline/ package refactor (rcp.py, ledger.py, stages.py, cli.py) — proven live: `pipeline run --random` reaches Gate A, `pipeline status` shows ledger truth. Learning system added by Jayon in parallel session (.agents/skills/learn/, docs/learning_system/).

Earlier: 2026-07-18: Execution plan locked (E1-E7 tasks, single source of truth). 2026-07-17: C2 v1 shipped — 3-skill chain + harness, first episode passed. Content strategy, Flow research, pipeline map. 2026-07-15: Canon names finalized. 2026-07-14: THE PIVOT to V2. Prior: B0-B2 complete, research library built.

## Learning log

- Covered: PSB method, evidence-chained decisions, git/gh, Docker+n8n basics, Supabase/PostgREST, structured outputs + semantic validate→retry (incl. two real failure lessons: -eln stemming false positive, max_tokens truncation). NEW: RCP architecture (why stateless-with-shared-pack beats one-long-conversation), hash-verified canon (tamper detection), ledger-based run tracking (resumability + auditability), Gate A pattern (human choice before committing resources).
- Covered (2026-07-21): single-responsibility skill splitting (one behavior = one versioned file, so `/tune` owns exactly one place); model-tier routing (Haiku 4.5 for cheap strict quality-checks — the R10 rubber-stamping risk, the "fail if unsure" mitigation, and the one-line model-upgrade path once `_call` is parameterized); dual video-model prompt design (Seedance first-30-words law + ref-mirroring vs Omni narrative brief + stateful edit-turns); reference-asset role mapping and resolving canonical names → assets (umlaut folding) with honest `pending` for assets not yet produced (don't fabricate); structured-output JSON schema discipline (`additionalProperties:false` + all-required).
- Covered (2026-07-21, E6): the "16k+ max_tokens" truncation lesson recurring at a new output shape (dual packages need proportionally more room — always re-check the ceiling when an output shape grows); a cost-tracking bug that was silently 10x wrong for months (E3-era code) — cents-per-token math is easy to get an order of magnitude wrong; per-model-tier pricing (don't apply Sonnet rates to Haiku calls); why `choose`/`resume` must reload by exact word positions, not re-fetch sequentially, once `--random` runs exist.
- Covered (2026-07-21, visual pivot): semantic poisoning — aesthetic vocabulary in a style block steers the whole latent space (old canon's "puppet/felt" words CAUSED seams/stop-motion looks; purge + AVOID-list both needed); Constants-vs-Variables prompt architecture (immutable medium/material/negative laws vs per-scene lighting/DoF written by the LLM-as-DP — the fix for "pasted-in" characters); PBR material vocabulary as identity control (IOR, subsurface scattering, albedo, ambient occlusion, displacement, anisotropic shading); multi-angle character sheets beat portraits for video refs (structural map prevents back/side dissolution); budget accounting when placeholders expand post-generation (LLM can't see final length — tell it the expansion cost).
- Up next: Jayon's Flashboard material-law test → (pass) style-lock image + audio voices + generate one full episode's video → assemble → post = full-loop proof. Then return to story-quality redesign (idea #16 + Jayon's parked design concerns). Merge `feat/e5-skills-v2` to main first.