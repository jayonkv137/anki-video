---
description: File an idea into the parking lot in one move — no discussion, back to work
---

Idea to file: $ARGUMENTS

Append one row to `docs/planning/IDEAS_PARKING_LOT.md`: next number, today's date, the idea's essence in ONE line (sharpen the phrasing, keep the intent), and a concrete revisit trigger (an event, not a date, unless a date is stated). Do NOT discuss, evaluate, or expand the idea. Commit with `docs: park idea #<n>`. Confirm to the user in one line, then return to the current work.
