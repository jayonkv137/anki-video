---
description: Update the living docs (changelog, project status, architecture) from recent work
---

Review what changed recently (git log + diff since the last changelog entry, plus this conversation) and update the living docs:

1. `docs/changelog.md` — add a dated entry for meaningful changes (skip trivia).
2. `docs/project_status.md` — tick checklist items, rewrite "Where we left off" so a cold session (or Antigravity) can resume without this chat, and update the learning log with concepts Jayon covered this session.
3. `docs/architecture.md` — only if structure actually changed.

Keep entries short and factual. Do not rewrite history — append. Then show me a summary of what you updated (don't commit unless I ask).
